package com.taptech.common.exception

import org.slf4j.Logger
import org.slf4j.LoggerFactory
import spock.lang.Specification

class ReactiveExceptionHandlerTest extends Specification {
    private static final Logger logger = LoggerFactory.getLogger(ReactiveExceptionHandlerTest.class);

    /*
    ./mvnw clean test -Dtest=UserContextPermissionsTest

     */

    def setup() {

    }          // run before every feature method
    def cleanup() {}        // run after every feature method
    def setupSpec() {

    }     // run before the first feature method
    def cleanupSpec() {
    }   // run after
}
